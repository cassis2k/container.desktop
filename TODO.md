# TODO

## Network View

- [ ] Improve "IN USE" / "NOT IN USE" badge accuracy: currently based on network state (running/created), but should check if containers are actually attached to the network. Requires implementing container listing API and cross-referencing network attachments.
